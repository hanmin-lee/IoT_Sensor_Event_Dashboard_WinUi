<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="IoT_Sensor_Event_Dashboard_WinUi.Pages.DataQueryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:IoT_Sensor_Event_Dashboard_WinUi.Pages">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Data Query" Style="{StaticResource Heading}" Margin="24,24,24,16"/>

        <ScrollViewer Grid.Row="1" Padding="24,0,24,0">
            <Border Style="{StaticResource Card}" Padding="20">
                <StackPanel>
                    <TextBlock Text="Search Results" Style="{StaticResource Heading}" Margin="0,0,0,10"/>
                    <ListView x:Name="listViewQueryResult" Background="Transparent" BorderThickness="0"/>
                </StackPanel>
            </Border>
        </ScrollViewer>

        <Border Grid.Row="2" Style="{StaticResource Card}" Padding="15" Margin="24,16,24,66">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Spacing="10">
                <Button x:Name="buttonQueryFirst" Content="First" MinWidth="80"/>
                <Button x:Name="buttonQueryPrev" Content="Previous" MinWidth="80"/>
                <TextBlock x:Name="labelQueryPageInfo" Text="Page 1 / 1" VerticalAlignment="Center"/>
                <Button x:Name="buttonQueryNext" Content="Next" MinWidth="80"/>
                <Button x:Name="buttonQueryLast" Content="Last" MinWidth="80"/>
            </StackPanel>
        </Border>

        <!-- Hidden controls kept for code-behind compatibility -->
        <StackPanel Visibility="Collapsed">
            <DatePicker x:Name="datePickerQueryStartTime"/>
            <TimePicker x:Name="timePickerQueryStartTime"/>
            <DatePicker x:Name="datePickerQueryEndTime"/>
            <TimePicker x:Name="timePickerQueryEndTime"/>
            <TextBox x:Name="textBoxQueryKeyword"/>
            <ComboBox x:Name="comboBoxQueryStatus">
                <ComboBoxItem Content="All"/>
                <ComboBoxItem Content="OK"/>
                <ComboBoxItem Content="ERROR"/>
            </ComboBox>
            <Button x:Name="buttonQuerySearch" Content="Search"/>
        </StackPanel>
    </Grid>
</Page>


