<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="IoT_Sensor_Event_Dashboard_WinUi.Pages.DataQueryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:IoT_Sensor_Event_Dashboard_WinUi.Pages">

    <Grid RowSpacing="24" Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Spacing="4">
            <TextBlock Text="Data Query" Style="{StaticResource Heading.Large}"/>
            <TextBlock Text="Review historical sensor data" Style="{StaticResource Caption}"/>
        </StackPanel>

        <Border Grid.Row="1" Style="{StaticResource Card}" Margin="0">
            <StackPanel Spacing="16">
                <TextBlock Text="Search Results" Style="{StaticResource Heading}"/>
                <ListView x:Name="listViewQueryResult"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource Card}" Margin="0,0,0,24">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Spacing="12">
                <Button x:Name="buttonQueryFirst" Content="First" MinWidth="96" Style="{StaticResource Button.Outlined}"/>
                <Button x:Name="buttonQueryPrev" Content="Previous" MinWidth="96" Style="{StaticResource Button.Outlined}"/>
                <TextBlock x:Name="labelQueryPageInfo" Text="Page 1 / 1" VerticalAlignment="Center"/>
                <Button x:Name="buttonQueryNext" Content="Next" MinWidth="96" Style="{StaticResource Button.Outlined}"/>
                <Button x:Name="buttonQueryLast" Content="Last" MinWidth="96" Style="{StaticResource Button.Outlined}"/>
            </StackPanel>
        </Border>

        <!-- Hidden controls kept for code-behind compatibility -->
        <StackPanel Visibility="Collapsed">
            <DatePicker x:Name="datePickerQueryStartTime"/>
            <TimePicker x:Name="timePickerQueryStartTime"/>
            <DatePicker x:Name="datePickerQueryEndTime"/>
            <TimePicker x:Name="timePickerQueryEndTime"/>
            <TextBox x:Name="textBoxQueryKeyword"/>
            <ComboBox x:Name="comboBoxQueryStatus">
                <ComboBoxItem Content="ALL"/>
                <ComboBoxItem Content="OK"/>
                <ComboBoxItem Content="ERROR"/>
            </ComboBox>
            <Button x:Name="buttonQuerySearch" Content="Search"/>
        </StackPanel>
    </Grid>
</Page>
