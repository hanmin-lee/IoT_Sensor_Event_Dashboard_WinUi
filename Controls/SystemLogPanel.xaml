<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="IoT_Sensor_Event_Dashboard_WinUi.Controls.SystemLogPanel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:IoT_Sensor_Event_Dashboard_WinUi.Controls">

    <Border Style="{StaticResource Card}" Padding="0">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Stretch" Margin="16,16,16,8">
                <TextBlock Text="System Log" Style="{StaticResource Heading}"/>
                <StackPanel Orientation="Horizontal" Spacing="8" HorizontalAlignment="Right">
                    <Button x:Name="btnLogClear" Content="Clear" Margin="17, 0, 0, 0" Padding="5" Style="{StaticResource Button.Outlined}"/>
                </StackPanel>
            </StackPanel>

            <TextBox x:Name="textBoxLog" Grid.Row="1" IsReadOnly="True" TextWrapping="Wrap" AcceptsReturn="True" 
                     ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.HorizontalScrollBarVisibility="Auto"
                     Background="Transparent" BorderThickness="0" FontFamily="Consolas" FontSize="12" Margin="16,0,16,16"/>
        </Grid>
    </Border>
</UserControl>







