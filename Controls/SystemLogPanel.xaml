<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="IoT_Sensor_Event_Dashboard_WinUi.Controls.SystemLogPanel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:IoT_Sensor_Event_Dashboard_WinUi.Controls">

    <Grid Padding="28,24,28,28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Spacing="8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" Spacing="12" VerticalAlignment="Center">
                    <TextBlock Text="System Log" Style="{StaticResource Heading}"/>
                    <Border Background="{StaticResource Brush.Primary.Soft}" CornerRadius="6" Padding="6,2">
                        <TextBlock Text="LIVE" Foreground="{StaticResource Brush.Primary}" FontWeight="SemiBold" FontSize="12"/>
                    </Border>
                </StackPanel>
                <Button x:Name="btnLogClear"
                        Grid.Column="1"
                        Content="Clear Log"
                        Style="{StaticResource Button.Outlined}"/>
            </Grid>
            <TextBlock Text="Real-time feed from the consumer background service." Style="{StaticResource Caption}"/>
        </StackPanel>

        <TextBox x:Name="textBoxLog"
                 Grid.Row="1"
                 IsReadOnly="True"
                 TextWrapping="Wrap"
                 AcceptsReturn="True"
                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                 ScrollViewer.HorizontalScrollBarVisibility="Auto"
                 Background="{StaticResource Brush.Surface}"
                 BorderBrush="{StaticResource Brush.Border}"
                 BorderThickness="1"
                 CornerRadius="14"
                 FontFamily="Consolas"
                 FontSize="12"
                 Padding="16"
                 Margin="0,12,0,0"
                 MinHeight="260"/>
    </Grid>
</UserControl>
