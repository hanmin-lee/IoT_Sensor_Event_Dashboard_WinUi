<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="IoT_Sensor_Event_Dashboard_WinUi.Controls.SystemLogPanel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:IoT_Sensor_Event_Dashboard_WinUi.Controls">

    <Border Style="{StaticResource Card}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Vertical" Spacing="4">
                    <TextBlock Text="System Log" Style="{StaticResource Heading}"/>
                    <TextBlock Text="Live activity" Style="{StaticResource Caption}"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                    <Button x:Name="btnLogClear" Content="Clear" Padding="10,6" Style="{StaticResource Button.Outlined}"/>
                </StackPanel>
            </Grid>

            <TextBox x:Name="textBoxLog"
                     Grid.Row="1"
                     IsReadOnly="True"
                     TextWrapping="Wrap"
                     AcceptsReturn="True"
                     ScrollViewer.VerticalScrollBarVisibility="Auto"
                     ScrollViewer.HorizontalScrollBarVisibility="Auto"
                     Background="#0D182C"
                     BorderBrush="{StaticResource Brush.Border}"
                     BorderThickness="1"
                     FontFamily="Consolas"
                     FontSize="12"
                     Margin="0"
                     MinHeight="240"/>
        </Grid>
    </Border>
</UserControl>
